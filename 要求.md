一个宝箱，需要用三把钥匙打开，总共有三个任务。每个人物可以获得一把钥匙

任务一：打字游戏：打字一分钟，要求打字速度超过每分钟 70字

任务二：用C++语言设计一个算法题，找出爱心密码（答案：20250614）
- 题目：程序自动读取三个数组（年份、月份、日期），新郎需要补全筛选算法的4个空缺
- 输入输出案例：
  - 年份数组: [5201314, 2025, 1314, 12345, 520]
  - 月份数组: [6, 13, 25, 99, 1]
  - 日期数组: [14, 32, 100, 30, 520]
  - 预期输出: 20250614
- 填空题要求：补全代码中标记的4个空位（共4处下划线）
- 筛选条件：年份=四位数(1000-9999)，月份=1-12，日期=1-31
- 代码编辑器支持Tab键自动补全和实时语法高亮

任务三：解答imgs\任务三.png的题目（到时候直接放图片就好了） - 答案为 5201314

每个任务完成可获得一把钥匙，完成后可开启宝箱。

通关密码：
- 任务一：LOVEU
- 任务二：SWEET
- 任务三：HONEY

宝箱打开后出现爱心页面，最后出现最终问题：
还记得我们第一次相遇的日子吗？



#include <bits/stdc++.h>
using namespace std;

// 判断年份是否为闰年
bool isLeapYear(int year) {
    return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);
}

// 获取某年某月的天数
int getDaysInMonth(int year, int month) {
    if (month == 2) {
        return isLeapYear(year) ? 29 : 28;
    }
    if (month == 4 || month == 6 || month == 9 || month == 11) {
        return 30;
    }
    return 31;
}

// 检查日期是否有效
bool isValidDate(int year, int month, int day) {
    if (year < 1000 || year > 9999) return false;
    if (month < 1 || month > 12) return false;
    if (day < 1 || day > 31) return false;
    if (day > getDaysInMonth(year, month)) return false;
    return true;
}

int main() {
    vector<int> years;
    vector<int> months;
    vector<int> days;

    // 读取年份数组
    string line;
    getline(cin, line);
    stringstream ss1(line);
    int num;
    while (ss1 >> num) {
        years.push_back(num);
    }

    // 读取月份数组
    getline(cin, line);
    stringstream ss2(line);
    while (ss2 >> num) {
        months.push_back(num);
    }

    // 读取日期数组
    getline(cin, line);
    stringstream ss3(line);
    while (ss3 >> num) {
        days.push_back(num);
    }

    // 查找最大的有效日期
    int maxYear = -1, maxMonth = -1, maxDay = -1;
    long long maxDateNum = -1;
    bool found = false;

    for (int y : years) {
        for (int m : months) {
            for (int d : days) {
                if (isValidDate(________)) {      // 填空1
                    found = true;
                    long long dateNum = y * 10000LL + m * 100 + d;
                    if (dateNum ________ maxDateNum) {   // 填空2
                        maxYear = y;
                        maxMonth = m;
                        maxDay = d;
                        maxDateNum = ________;          // 填空3
                    }
                }
            }
        }
    }

    // 输出结果
    if (!found) {
        cout << 0;
    } else {
        cout << ________;                            // 填空4
    }

    return 0;
}




填空	答案	作用
填空1	y, m, d	调用 isValidDate 验证日期是否有效
填空2	>	比较找最大日期
填空3	dateNum	更新最大日期
填空4	maxYear * 10000 + maxMonth * 100 + maxDay